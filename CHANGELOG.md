# Changelog

All notable changes to this project are documented here.

## 0.1.0 - Unreleased

- Implement the complete frozen `iso8601==2.1.0` namespace and behavior.
- Add a Rust/PyO3 lexical parser with exact Python object compatibility wrapper.
- Add a Python 3.7-compatible universal fallback wheel.
- Add frozen-upstream, 10,000-case differential, artifact, benchmark, CI and
  fail-closed release gates.

