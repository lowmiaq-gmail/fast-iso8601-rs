# fast-iso8601-rs 指南 (Agent Guide)

## 1. 本层级核心职责

实现并验证 `iso8601==2.1.0` 的 Rust-backed 完整 drop-in replacement，同时保留 Python 3.7 可用的 pure-Python fallback。

## 2. 下级目录递归映射 (Sub-Directory Map)

- `/upstream`: 由固定 PyPI sdist 独立冻结的 oracle 源码与完整测试。
- `/python`: native mixed-project 的 Python compatibility layer。
- `/fallback`: universal wheel 的 pure-Python implementation。
- `/src`: Rust/PyO3 高价值解析实现。
- `/tests`: candidate contract 与 artifact tests。
- `/scripts`: differential、artifact、benchmark 与构建 gate。
- `/docs`: release、SEO 与验证证据。
- `/.github`: packaged-wheel CI 与 guarded release workflows。

## 3. 运行、测试与输出规范

- **允许的入口文件**: `pyproject.toml`, `Cargo.toml`, `scripts/`, `.github/workflows/`。
- **临时数据存放点**: ignored `.venv/`, `target/`, `.pytest_cache/`, `dist/`, `.benchmarks/` 以及系统临时目录。

## 4. 阶段性清理清单 (Cleanup Checklist)

- [ ] 检查并移除无用的临时导出文件。
- [ ] 确保无未跟踪的垃圾代码残留。

## 5. 渐进式发现日志 (Progressive Discovery Log)

- [2026-08-12] 锁定 `micktwomey/pyiso8601` 2.1.0；完整兼容范围包括公开与可导入 submodule contract、异常链、timezone identity、typing 与 artifact/release gates。
